import { ProductoHabilitadoPipe } from './producto-habilitado.pipe';

describe('ProductoHabilitadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoHabilitadoPipe();
    expect(pipe).toBeTruthy();
  });
});
