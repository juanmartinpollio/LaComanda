import { UsuarioTipoPipe } from './usuario-tipo.pipe';

describe('UsuarioTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
