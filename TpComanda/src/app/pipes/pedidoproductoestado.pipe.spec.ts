import { PedidoproductoestadoPipe } from './pedidoproductoestado.pipe';

describe('PedidoproductoestadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidoproductoestadoPipe();
    expect(pipe).toBeTruthy();
  });
});
