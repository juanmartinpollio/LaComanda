import { ProductoSectorPipe } from './producto-sector.pipe';

describe('ProductoSectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoSectorPipe();
    expect(pipe).toBeTruthy();
  });
});
