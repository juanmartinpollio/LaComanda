import { MesaEstadoPipe } from './mesa-estado.pipe';

describe('MesaEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new MesaEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
