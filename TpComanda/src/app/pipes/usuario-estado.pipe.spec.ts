import { UsuarioEstadoPipe } from './usuario-estado.pipe';

describe('UsuarioEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
