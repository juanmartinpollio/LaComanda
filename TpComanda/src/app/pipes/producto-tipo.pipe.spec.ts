import { ProductoTipoPipe } from './producto-tipo.pipe';

describe('ProductoTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
